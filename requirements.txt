# Version ranges rather than exact pins: this has to install on whatever
# Python the marker happens to have (3.10 through 3.14 were tested), and a hard
# pin to a release with no wheel for their interpreter fails the build at the
# worst possible moment.

# --- Web layer ---------------------------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9
pydantic>=2.9,<3.0

# --- Configuration -----------------------------------------------------------
python-dotenv>=1.0,<2.0

# --- AI providers ------------------------------------------------------------
# All three are optional at runtime: if a package or its key is missing,
# IncidentIQ falls back to the offline heuristic engine instead of crashing.
# Each SDK is imported lazily, inside the client that needs it.
google-genai>=1.0
anthropic>=0.69
openai>=1.59

# --- Tests -------------------------------------------------------------------
pytest>=8.0
httpx>=0.27
